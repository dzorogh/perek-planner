---
scope: perek-planner initiative architecture spine
purpose: build substrate for independent feature/epic implementation without drift
altitude: initiative
updated: 2026-07-19T23:42
---

- (event) Create run started; coaching path; deliverable=spine only; altitude=initiative
- (constraint) Sources: PRD prd-perek-planner-2026-07-19 + UX ux-perek-planner-2026-07-19 (binding product/UX decisions)
- (constraint) UI stack from UX [ADOPTED]: desktop web RU; Next.js + Tailwind + shadcn/ui; Soft Workshop + Lavender Workshop; light-only; Geist Sans
- (constraint) Product shape: personal hobby single-operator; auth login/password; Perekrestok store catalog sync; AI recipe suggestions gated by checked matches + today-stock; shopping handoff=copy (+ optional store link); no in-app cart edit
- (decision) Shape: Next.js UI + BaaS for data/auth/catalog persistence (not all-in-one monolith, not custom multi-service). Divergence prevented: one epic putting domain DB in Next-only while another invents a separate API/DB.
- (direction) Coaching next: pin BaaS product; place AI suggestions + Perekrestok catalog sync relative to Next vs BaaS
- (decision) BaaS pinned: Supabase (Auth+Postgres+RLS). Divergence prevented: mixing Firebase/PocketBase/custom auth+DB stacks.
- (decision) Catalog sync owner: Supabase (pg_cron / scheduled Edge Function writes catalog snapshot). User rejected Next scheduled route for sync. Divergence prevented: one epic syncing via Vercel cron while another uses Supabase jobs.
- (direction) User wants 'everything related to catalog sync' colocated with Supabase cron; clarify AI + eligibility orchestration boundary Next vs Edge
- (decision) Boundary: Catalog sync pipeline owns write of Products/availability/stale markers (Supabase Cron+Edge Function); Next reads only. AI suggestions + Menu/Portion/Shopping orchestration live in Next server. Divergence prevented: dual writers of catalog; AI logic split across Deno Edge and Next.
- (direction) Coaching next: Checked-match/eligibility ownership; Menu source of truth; store-adapter shape
- (decision) Checked-match + today-stock eligibility logic lives in Next (server). Supabase stores inputs (catalog snapshot, recipes/ingredients) and outputs (Menu, persisted matches if any) — not the matching rules engine. Divergence prevented: DB functions vs Next both owning eligibility.
- (direction) Coaching next: match persistence; store-adapter for Perekrestok; Menu SoT
- (decision) Checked matches are persisted in Supabase when resolved; Shopping list and Menu reopen use stored matches (not recompute-every-time). Divergence prevented: ephemeral match results vs durable match rows as SoT for list/handoff.
- (direction) Coaching next: Perekrestok store-adapter / how catalog is obtained; Menu SoT (likely Supabase — confirm)
- (constraint) Catalog source candidate: unofficial Python client perekrestok-api (PyPI 0.2.2, Feb 2026) — mimics site traffic; not official Perekrestok API
- (question) Sync runtime conflict: user wanted Supabase Cron+Edge for catalog writes, but chosen client is Python-only — need host for Python adapter that still writes only to Supabase catalog tables
- (constraint) Deploy host: Dokploy (not GitHub Actions). App runtime lives there.
- (direction) Reconcile: Python catalog sync + cron on Dokploy; clarify if Supabase is cloud or also on Dokploy
- (decision) Deploy: Dokploy hosts Next app + Python catalog-sync container; Dokploy Schedule Jobs cron runs sync (not GitHub Actions, not Supabase Cron for catalog). Supabase = cloud (supabase.com) for Auth+Postgres+RLS. Divergence prevented: split hosting of app vs inventing Actions/Edge cron for sync.
- (decision) Store adapter v1: unofficial Python perekrestok-api inside sync worker; writes normalized catalog into Supabase only. Divergence prevented: Next or Edge also fetching store APIs.
- (direction) Coaching next: AI/LLM provider; Menu SoT confirm; env strategy (prod-only vs staging)
- (decision) LLM: Next server calls OpenRouter; model is a cheap configurable id (not pinned in architecture). API key server-only. Divergence prevented: client-side LLM calls or hardcoding a single vendor SDK as the only path.
- (assumption) Exact OpenRouter model id left as runtime config / seed default at implement time
- (decision) Environments: single prod on Dokploy + one Supabase cloud project; local Next/Python for development. No staging. Divergence prevented: parallel env matrices invented per epic.
- (decision) Menu/Recipe/Rating/Refusal/Portion/Shopping outputs + persisted Checked matches SoT: Supabase Postgres. Next mutates via authenticated server clients; catalog tables write-gated to sync service role only.
- (decision) Auth: Supabase Auth email/password (FR-23); session via @supabase/ssr cookies in Next. Single-operator; RLS denies cross-user access even if multi-user later.
- (decision) Paradigm: BaaS-backed modular Next — UI+domain orchestration in Next modules; persistence/auth in Supabase; store ingest as separate Python sync worker. Dependency rule: app → supabase client; sync → supabase service; neither Next nor sync imports the other's internals.
- (event) Elicitation closed by user (хватит); proceeding to distill+finalize; deliverable remains spine only
- (change) Supersedes earlier Supabase Cron+Edge catalog sync: schedule is Dokploy Schedule Jobs on Python worker; Edge Functions not used for catalog ingest
- (decision) Shared contracts: Supabase migrations are schema SoT; TypeScript types generated/derived from schema — no duplicated hand-maintained DTOs across Next and sync.
- (decision) CheckedMatch: normalized persisted rows owned by matching domain (not JSON blob on MenuSlot). Shopping list derived at handoff from those rows (+ default staple candidates per UX); matches remain SoT for products on the list.
- (decision) Store context: user settings hold selected_store_id (default Alabino 92); Menu snapshots store_id at creation; sync refreshes catalog for configured store(s). Clone/reuse Menu re-runs matching — does not copy stale CheckedMatches.
- (decision) Eligibility timing: gate at suggest/assign; persisted matches frozen for that Menu handoff; slot replace re-matches. Stale catalog warning is orthogonal (FR-18).
- (constraint) UX overrides binding for v1: History (not Recipe library browse) primary; staples/candidates on Shopping list by default (no per-item pantry prompt); store picker in Settings; light-only desktop RU; no in-app cart edit
- (decision) Suggestion memory: Refusal and dislike Rating hard-suppress that Recipe/Snack from future AI suggestions (PRD FR-8/FR-10) — Next suggestion path must apply before OpenRouter or post-filter consistently in one module.
- (event) Reviewer Gate applied: AD-7..AD-10 + tightened AD-6/AD-4; starter Tailwind upgrade note; UX constraints in conventions
- (event) spine finalized
- (decision) Shopping list: materialize snapshot at buildShoppingList from Menu CheckedMatches + default staples; no in-app edit; regenerate after match/slot changes. Divergence prevented: virtual vs persisted list SoT clash.
